// This a file shared among all the pages within this chapter.
// Any files in the Sources folder will be available to all pages.

public var str = "Hello from Make School!"

public func sharedFunc() {
    print(str)
}
